docker-compose -f docker-compose.yml up -d --build --force-recreate

docker-compose -f docker-compose.yml up -d --build --force-recreate game-server

docker-compose -f docker-compose.yml up -d --build --force-recreate worker-sync